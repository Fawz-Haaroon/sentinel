services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../app:/app/app:rw
      - ../configs:/app/configs:rw
    environment:
      - PYTHONUNBUFFERED=1

